<div class="mix y{{anio}} thumbnail slider-item" data-layer="{{leaflet_id}}" data-myorder="{{@index}}">
    <a class="imagen" href="#">
        <img src="{{imagen}}">
        <div class="caption">
            <span class="titulo">{{titulo}}</span>
        </div>
    </a>
</div>