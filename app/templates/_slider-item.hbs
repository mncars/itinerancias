<div class="mix y{{anio}} thumbnail slider-item" data-myorder="{{@index}}">
    <a class="imagen" href="#">
        <img src="{{imagen}}">
        <div class="caption">
            <span class="titulo">{{titulo}}</span>
        </div>
    </a>
</div>